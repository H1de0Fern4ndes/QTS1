function buscarCursoNoBanco(id) {
  // Imagine que isso acessa banco/API
  return { nome: 'João Mockado' };
}

function buscarPeriododoCurso(id) {
  // Imagine que isso acessa banco/API
  return { periodo: 'Vespertino' };
}

module.exports = {
  buscarCursoNoBanco,
  buscarPeriododoCurso
};